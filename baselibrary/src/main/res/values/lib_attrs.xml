<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 轮播指示 -->
    <declare-styleable name="BannerIndicator">
        <attr name="indicatorMargin" format="dimension"/>
        <attr name="indicatorDrawableRes" format="reference"/>
    </declare-styleable>

    <!-- WheelView -->
    <declare-styleable name="WheelView">
        <!-- 中间分割线外的item数量,整个滑动数量就为 wheelItemCount * 2 + 1  -->
        <attr name="wheelItemCount" format="integer"/>
        <!-- 滑轮item高度 -->
        <attr name="wheelItemSize" format="dimension"/>
        <!-- 滑轮字体大小 -->
        <attr name="wheelTextSize" format="dimension"/>
        <!-- 滑轮字体颜色 -->
        <attr name="wheelTextColor" format="color"/>
        <!-- 滑轮中心字体颜色 -->
        <attr name="wheelTextColorCenter" format="color"/>
        <!-- 分割线颜色 -->
        <attr name="dividerColor" format="color"/>
        <!-- 布局方向 -->
        <attr name="wheelOrientation">
            <enum name="vertical" value="1"/>
            <enum name="horizontal" value="2"/>
        </attr>
        <!-- 水平布局时不考虑只参数,  当垂直布局时的左右靠齐立体效果 -->
        <attr name="wheelGravity">
            <enum name="left" value="1"/>
            <enum name="center" value="2"/>
            <enum name="right" value="3"/>
        </attr>

        <!-- 两根分割线的距离 -->
        <attr name="wheelDividerSize" format="dimension"/>
    </declare-styleable>

    <!-- 按宽高比例布局 -->
    <declare-styleable name="scaleRelativeLayout">
        <attr name="widthScale" format="float"/>
    </declare-styleable>

    <declare-styleable name="CharIndexView">
        <!-- 	banner背影色  	 -->
        <!-- 索引字大小 -->
        <attr name="indexTextSize" format="dimension"/>
        <!-- 字颜色 -->
        <attr name="charTextColor" format="color"/>
        <!-- 索引字颜色 -->
        <attr name="indexTextColor" format="color"/>
    </declare-styleable>

    <declare-styleable name="SpecialEditText">
        <attr name="specialMaxLength" format="integer"/>
    </declare-styleable>

    <!-- 最大宽度 -->
    <declare-styleable name="MaxLinearLayout">
        <attr name="layoutMaxLength" format="dimension"/>
    </declare-styleable>

</resources>